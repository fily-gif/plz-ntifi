server="" # full server url! (with http(s)://)
api_key="" #* jellyfin api key; backend automatically appends mediabrowser and stuff
#(the token is at the very bottom!)







































bot_token="" # discord bot token

slack_webhook="" #* currently unused by ntifi/bot/slack (see NOTE below); kept for reference/other tools
# slack bot needs an actual app, not just a webhook, since only chat.postMessage/.update
# hand back a message `ts` to thread replies off of / edit in place. create an app at
# https://api.slack.com/apps, enable socket mode + slash commands, then fill these in:
slack_bot_token="" # xoxb-... (scopes: chat:write, users:read, commands)
slack_app_token="" # xapp-... (socket mode, scope: connections:write)
slack_owner_id="" # optional; a slack user id that's always treated as authorized